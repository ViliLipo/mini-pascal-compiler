program greatest_divisor;
begin
  var i: array[5] of integer;
  var d: integer;
  i[2] := 3;
  d := (i[2] + 5) * (1000/3);
  if d < 1000 then writeln(i) else
  begin
    var x: integer;
    x := 3 + 8;
    i[2] := i[2] + x;
    writeln(i)
  end
end.
