program greatest_divisor;
begin
  var i: integer;
  var d: integer;
  i := 3;
  d := i + (5 * (1000/3));
  if d < 1000 then 
  writeln(i);
  else
  begin
    var x: integer;
    x := 3 + 8;
    i := i + x;
    writeln(i);
  end
end.
